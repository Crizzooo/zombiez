
//Constant Action Types


//Action Creators

const game = (state = initialState, action) => {
  let newState = Object.assign({}, state);

  switch(action.type) {

    default:
      return state;
  }
}

module.exports = { game };
